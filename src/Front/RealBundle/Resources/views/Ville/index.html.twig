{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Ville list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Ville</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('ville_show', { 'id': entity.ville }) }}">{{ entity.ville }}</a></td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('ville_show', { 'id': entity.ville }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('ville_edit', { 'id': entity.ville }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('ville_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
